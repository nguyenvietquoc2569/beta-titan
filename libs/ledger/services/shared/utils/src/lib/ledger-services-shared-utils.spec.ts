import { ledgerServicesSharedUtils } from './ledger-services-shared-utils';

describe('ledgerServicesSharedUtils', () => {
  it('should work', () => {
    expect(ledgerServicesSharedUtils()).toEqual('ledger-services-shared-utils');
  });
});
